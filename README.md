# Git aliases/functions
Bash git command aliases and git-based tools for commonly used git commands

# Installation
Using basherbee (recommended): ```basherbee install chetanc10/gitbit```  
Using basher: ```basher install chetanc10/gitbit```

# gitmod
This is a bash executable which helps go through all modified files in a git repo and takes action as per user's choice per file
